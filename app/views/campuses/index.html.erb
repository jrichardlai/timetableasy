<h1>Listing campuses</h1>

<table>
  <tr>
  </tr>

<% @campuses.each do |campus| %>
  <tr>
    <td><%= link_to 'Show', campuses %></td>
    <td><%= link_to 'Edit', edit_campuses_path(campuses) %></td>
    <td><%= link_to 'Destroy', campuses, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New campuses', new_campuses_path %>