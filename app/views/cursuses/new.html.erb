<h1>New cursus</h1>

<% form_for(@cursus, :url => cursuses_path) do |f| %>
  	<%= f.error_messages %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<br />
	<%= f.label :description %>
	<br />
	<%= f.text_area :description %>
	<br />
	<%= f.label :campus %>
	<br />
	<%= f.text_area :campus %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', cursuses_path %>